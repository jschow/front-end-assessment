angular
  .module('assessment')
  .service('mainService', function($http, $q) {

    /*var shopUrl = 'https://dev-assessment.firebaseio.com/products.json';

    this.shopData = function() {
      var deferred = $q.defer();
      $http.get(shopUrl).then(function(response) {
        var shopData = response.data;
        deferred.resolve(shopData);
      });
      return deferred.promise;
    };*/
    
  }); // end mainService
